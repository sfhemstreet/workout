import { WorkoutContainer } from "../containers/WorkoutContainer";

/**
 * Workout page is where a user follows along and completes a workout.
 */
export default function WorkoutPage() {
  return <WorkoutContainer />;
}
